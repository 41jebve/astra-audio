# 本仓库用于存放 Astra 语音音频，故意不启用 Git LFS。
# 原因：Git LFS 免费额度仅 1 GB，而本仓库音频总量约 1.16 GiB，会直接爆额度。
# 因此所有音频都以普通 Git 对象提交。

# 音频一律按二进制处理：不做差异比较、不做合并、不转换换行符
*.mp3 binary
*.wav binary

# 双保险：即使全局配置里启用了 LFS 过滤器，本仓库也强制不带 filter
*.mp3 -filter
*.wav -filter
